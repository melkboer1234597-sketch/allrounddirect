# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules

# Build
dist
dist-ssr
dist-worker
*.local

# Environment – secrets never in git (.env.example is allowed)
.env
.env.*
!.env.example
!.env.import.example
.dev.vars
!.dev.vars.example

# Catalog import cache (optimized images, never source scrape)
.cache/

# Large dry-run JSON stays local; markdown summaries may be committed
data/import-reports/*.json


# Cloudflare / Wrangler
.wrangler/
.mf

# Drizzle migrations output (keep schema in source; generate when ready)
# drizzle/

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
Thumbs.db

# SSH private keys must never be committed (keys live outside this repo)
*_deploy
*.pem
id_rsa
id_ed25519
id_ecdsa
id_dsa
