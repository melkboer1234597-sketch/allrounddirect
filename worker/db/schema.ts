/**
 * Drizzle schema for Cloudflare D1 (SQLite).
 * Tables worden later toegevoegd (producten, orders, klanten, enz.).
 */
// import { sqliteTable, text, integer } from 'drizzle-orm/sqlite-core'

export {}
